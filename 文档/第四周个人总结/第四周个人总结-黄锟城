本周的主要工作是进行姿态评估部分的编码，首先是读取json中的动作序列，由于使用DTW算法比较的是关键点的空间距离，而人体的骨骼长度不一，
因此需要对两个动作序列的骨骼长度进行统一，由于比较动作的相似度时比较的是骨骼向量，与骨骼长度无关，所以可以直接保持骨骼向量方向并统一长度，
对骨骼长度统一采用的是第一个动作序列的平均值，由于关键点有多个，最终采用的是四肢上的关键点，而帧与帧之间的距离就是这些关键点的距离之和，进行匹配
时用的则是DTW算法，它采用动态规划的办法，获取距离之和最小帧匹配路径。
